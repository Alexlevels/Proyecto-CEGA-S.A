@model CapaPresentacion.Models.ComentarioProyecto
@{
    ViewBag.Title = "Agregar Comentario";
}

<h2>Agregar Comentario</h2>

@using (Html.BeginForm())
{
    @Html.HiddenFor(m => m.ProyectoId)

    <div class="form-group">
        @Html.LabelFor(m => m.Texto)
        @Html.TextAreaFor(m => m.Texto, new { @class = "form-control", rows = 4 })
    </div>

    <button type="submit" class="btn btn-primary">Guardar</button>
    <a href="@Url.Action("Index", new { proyectoId = Model.ProyectoId })" class="btn btn-secondary">Cancelar</a>
}
